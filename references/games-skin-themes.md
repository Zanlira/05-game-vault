# Skins por juego

Mantenido por el agente skin-designer.

| Juego     | Skins                | Fecha      | Notas                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------- | -------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| asteroids | clasico, neon, retro | 2026-07-11 | Paleta en `themes.ts` (`ASTEROIDS_THEMES`). Engine parametrizado (nave, asteroides, balas, partículas, power-up, HUD, overlay) vía `palette.*`; neon usa `ctx.shadowBlur/shadowColor` con `palette.glow`. `registry.ts` con `skin?: SkinId` en `GameProps`. `tsc --noEmit` OK. Solo verificado a nivel de tipos, no runtime.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| snake     | clasico, neon, retro | 2026-07-11 | Paleta en `themes.ts` (`SNAKE_THEMES`). `createSnakeGame` recibe `skin: SkinId = "clasico"` como 3er parámetro y resuelve `palette` una vez; fondo/grid/borde/cabeza-cuerpo/HUD/overlay de game-over vía `palette.*`; neon usa `ctx.shadowBlur/shadowColor` con `palette.glow` en borde y serpiente. `Snake.tsx` pasa `skin ?? "clasico"` y lo agrega a deps del `useEffect` de montaje. `tsc --noEmit` OK (errores preexistentes no relacionados en `arkanoid-engine.ts`, fuera de alcance). Solo verificado a nivel de tipos, no runtime.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| arkanoid  | clasico, neon, retro | 2026-07-11 | Paleta en `themes.ts` (`ARKANOID_THEMES`, extiende `GamePalette` con `blockColors: Record<string,string>` mapeando los nombres de color de `arkanoid-assets/levels.ts` a hex por skin). Los bloques ya no usan el sprite PNG: se dibujan como rects rellenos con `palette.blockColors[block.color]`. Paddle y ball dejaron de ser sprite (su tinte fijo chocaba con neon/retro) y ahora son formas simples (`palette.primary`/`palette.accent`); las explosiones siguen usando el sprite sheet (efecto decorativo breve, no choca). Fondo, overlays (pausa/game over/win), HUD y botones de salto de nivel parametrizados vía `palette.*`; neon aplica `ctx.shadowBlur/shadowColor` con `palette.glow` en bloques/paddle/ball. `createArkanoidGame` recibe `skin: SkinId = "clasico"` como 3er parámetro; `Arkanoid.tsx` lo propaga con fallback `"clasico"` y lo agregó a las deps del `useEffect` de montaje. `tsc --noEmit` OK. Solo verificado a nivel de tipos, no runtime. |
